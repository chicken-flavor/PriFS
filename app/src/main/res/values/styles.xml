<resources>

    <!-- Base application theme. -->
    <style name="AppThemeBase" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="toolbarNavigationButtonStyle">@style/ToolbarNavigationButtonStyle</item>
        <!--<item name="android:actionMenuTextColor">@android:color/white</item>-->
        <!--<item name="android:tint">@android:color/white</item>-->
    </style>

    <style name="AppTheme" parent="AppThemeBase" />

    <style name="AppThemeDayNightBase" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="toolbarNavigationButtonStyle">@style/ToolbarNavigationButtonStyle</item>
        <!--<item name="android:actionMenuTextColor">@android:color/white</item>-->
        <!--<item name="android:tint">@android:color/white</item>-->
    </style>

    <style name="AppThemeDayNight" parent="AppThemeDayNightBase" />

    <style name="ToolbarNavigationButtonStyle" parent="@style/Widget.AppCompat.Toolbar.Button.Navigation">
        <item name="android:minWidth">0dp</item>
        <item name="android:padding">@dimen/default_toobar_icon_padding</item>
        <item name="android:scaleType">centerInside</item>
    </style>

    <style name="AppTheme.ToolbarBase" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@android:color/white</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/toolbar_height</item>
        <item name="android:scaleType">centerCrop</item>
    </style>

    <style name="AppTheme.Toolbar" parent="AppTheme.ToolbarBase" />

    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">@dimen/toolbar_textsize</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Dialog（选择养护段） -->
    <style name="SelfDialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!--过滤选择弹窗-->
    <style name="FilterPopupAnima">
        <item name="android:windowEnterAnimation">@anim/popwin_top2bottom_scale_show</item>
        <item name="android:windowExitAnimation">@anim/popwin_top2bottom_scale_hiden</item>
    </style>

    <!-- 表单弹窗 -->
    <style name="FormPopupAnima">
        <item name="android:windowEnterAnimation">@anim/popwin_fade_show</item>
        <item name="android:windowExitAnimation">@anim/popwin_fade_hiden</item>
    </style>

    <style name="SwitchPopupAnima">
        <item name="android:windowEnterAnimation">@anim/popwin_bottom2top_trans_show</item>
        <item name="android:windowExitAnimation">@anim/popwin_bottom2top_trans_hiden</item>
    </style>

    <!-- 滚动选择器的 show & hiden  -->
    <style name="PickerPopupAnima">
        <item name="android:windowEnterAnimation">@anim/popwin_show_right2left</item>
        <item name="android:windowExitAnimation">@anim/popwin_hiden_left2right</item>
    </style>

    <!-- Marker过滤器的 show & hiden -->
    <style name="MarkerPopup">
        <item name="android:windowEnterAnimation">@anim/popwin_marker_in</item>
        <item name="android:windowExitAnimation">@anim/popwin_marker_out</item>
    </style>

    <!-- 列表样式 -->
    <style name="TextBase">
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">1</item>
    </style>
    <!-- 列表标题 -->
    <style name="ListTitle" parent="TextBase">
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@color/text_1st</item>
        <item name="android:textSize">@dimen/default_textsize</item>
    </style>
    <!-- 列表内容.标题 -->
    <style name="ListContent.Title" parent="TextBase">
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/text_2nd</item>
        <item name="android:textSize">@dimen/small_textsize</item>
    </style>
    <!-- 列表内容.内容 -->
    <style name="ListContent.Content" parent="TextBase">
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@color/text_2nd</item>
        <item name="android:textSize">@dimen/small_textsize</item>
    </style>

    <!-- 表单内容.标题 -->
    <style name="FormContent.Title" parent="TextBase">
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/text_2nd</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/default_textsize</item>
    </style>
    <!-- 表单内容.内容 -->
    <style name="FormContent.Content" parent="TextBase">
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/default_textsize</item>
    </style>

</resources>
